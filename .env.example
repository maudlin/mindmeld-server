# MindMeld Server environment variables
# Copy to .env and adjust values as needed

# Server
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://127.0.0.1:8080  # Default enables both localhost and 127.0.0.1 variants
JSON_LIMIT=50mb
LOG_LEVEL=debug

# Maps API (SQLite)
SQLITE_FILE=./data/db.sqlite
# FEATURE_MAPS_API defaults to enabled; set to 0/false to disable
FEATURE_MAPS_API=1

# MCP (Model Context Protocol) - enabled by default for AI assistant integration
# FEATURE_MCP=true  # Default: enabled
# MCP_TOKEN=         # Optional authentication token for MCP clients

# Legacy state file (for MCP state resource)
# STATE_FILE=./data/state.json

# Real-time Collaboration - enabled by default for full experience
# SERVER_SYNC=on         # Default: enabled (WebSocket server for real-time sync)
# DATA_PROVIDER=yjs      # Default: yjs (full collaborative features)
# VERIFY_YJS=false       # Set to 'true' or '1' to enable Y.js document verification (dev/debug)

# To disable collaboration features, set:
# SERVER_SYNC=off
# DATA_PROVIDER=json

# Monitoring and security (optional)
# MONITORING_HOSTS=10.0.1.100,192.168.1.50  # Additional IPs allowed to access /health/deep and /metrics
# Set NODE_ENV=production to enable stricter security for public endpoints
